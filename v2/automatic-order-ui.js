// Compatibility stub: supplier fulfillment is manual.
// Kept temporarily because the checkout loader still references this path.
window.FSAutomaticOrderUI = Object.freeze({ version: 'manual-only', refresh: () => {} });
